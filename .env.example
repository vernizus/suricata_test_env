# Network interface Suricata will monitor
#
# Linux — find your main interface:
#   ip route | grep default | awk '{print $5}'
#
# Windows Docker Desktop (WSL2):
#   Always "eth0" inside the container
#   (WSL2 virtual interface — not the physical Windows NIC)
#
SURICATA_IFACE=eth0
